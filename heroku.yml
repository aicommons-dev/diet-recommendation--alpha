build:
  docker:
    web: Dockerfile
  config:
    DJANGO_SETTINGS_MODULE: project.settings
run:
  web: gunicorn foodie.wsgi:application --bind 0.0.0.0:$PORT
release:
  image: web
  command:
    - python foodie manage.py collectstatic --noinput