build:
  docker:
    web: Dockerfile
run:
  web: gunicorn --chdir ./foodie foodie.wsgi:application --log-file --bind 0.0.0.0:$PORT
release:
  image: web
  command:
    - python manage.py collectstatic --noinput